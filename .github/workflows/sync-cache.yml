name: Auto-sync cache from Google Drive

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  sync-cache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # ← forces full clone so empty folders exist

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt

      - name: Generate Parquet cache
        run: |
          python generate_cache.py

      - name: Install rclone
        run: sudo apt-get update && sudo apt-get install -y rclone

      - name: Sync cache folder
        env:
          RCLONE_CONFIG_CONTENT: ${{ secrets.RCLONE_CONFIG }}
        run: |
          mkdir -p ~/.config/rclone
          echo "$RCLONE_CONFIG_CONTENT" > ~/.config/rclone/rclone.conf
          rclone sync "ZEF_Container_Dashboard:/Container Plant Dashboard/Parquet_Exports/cache" Parquet_Exports/cache --create-empty-src-dirs --progress || true

      - name: Commit and push if changed
        run: |
          git config user.name "Auto-sync Bot"
          git config user.email "bot@noreply.github.com"
          
          # Ensure the folder exists and is tracked
          mkdir -p Parquet_Exports/cache
          touch Parquet_Exports/cache/.gitkeep 2>/dev/null || true
          
          git add Parquet_Exports/cache/
          if git diff --staged --quiet; then
            echo "No changes in Parquet_Exports/cache"
          else
            git commit -m "Auto-update cache $(date +'%Y-%m-%d %H:%M')"
            git push
          fi